
* ## Exercise

~~~
Create a maven project in which to connect to the database, insert a record into this table and display the contents
There should be a main method to demonstrate functionality. Frameworks and libraries cannot be used, only JDBC API
~~~